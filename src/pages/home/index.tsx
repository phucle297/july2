import { Button } from "@mui/material";
import { FC } from "react";

const HomePage: FC = () => {
  return (
    <section>
      <h1>Home Page</h1>
      <Button>Mui Btn</Button>
    </section>
  );
};

export default HomePage;
